#include<iostream>
#include<cmath>
#include<vector>
using namespace std;
int main()
{   
    // q1.
    // int n = 4;

    // // std::cout<<std::pow(n,2)<<" "<<std::sqrt(n);

    // cout<<pow(n,2)<<endl;
    // cout<<sqrt(n)<<endl;

    vector<int> arr;
    arr.resize(10);
    for (int i = 0; i < 10; i++)
    {
        arr[i] = i+1;
    }
    

    return 0;
}